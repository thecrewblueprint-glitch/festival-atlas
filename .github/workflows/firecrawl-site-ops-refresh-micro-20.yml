name: Firecrawl Site Ops Refresh Micro 20 Sweep

on:
  push:
    branches:
      - research-version
    paths:
      - 'research/firecrawl-run-control.json'
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  site-ops-refresh-micro-20:
    name: Site Ops refresh micro 20 sweep
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      FIRECRAWL_API_KEY: ${{ secrets.FIRECRAWL_API_KEY }}
      MAX_TOTAL_CREDITS: '20'
      MAX_SEARCH_RESULTS: '1'
      OUTPUT_DIR: firecrawl-output-site-ops-refresh
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node tools/firecrawl-site-ops-refresh-runner.js
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: firecrawl-site-ops-refresh-micro-20
          path: firecrawl-output-site-ops-refresh/
          if-no-files-found: warn
          retention-days: 7
