(function(){
  // Inert. This overlay previously injected "Research Confidence: X/Y" badges with
  // filled/unknown field labels onto every festival card. That is internal research/audit
  // language and is no longer shown in the public UI — festival cards and modals now render
  // only useful public festival information and employer routes directly from atlas-core-v2.js.
  // Kept as a no-op so existing <script> references on each page continue to resolve.
})();
