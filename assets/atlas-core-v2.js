(function(){
  var MONTHS=['January','February','March','April','May','June','July','August','September','October','November','December'];
  var SHORT_MONTHS=['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
  var branchFiles=[];
  var branches=[];
  var allOpportunities=[];
  var opportunities=[];
  var employers=[];
  var iatseLocals=[];
  var branchIndex={records:[],byKey:{}};
  var branchDataReady=false;
  var SCH_KEY='production-atlas-schedule-v1';
  var UNKNOWN_PRODUCER='__unknown_promoter__';
  var STATE_ABBR={'Alabama':'AL','Alaska':'AK','Arizona':'AZ','Arkansas':'AR','California':'CA','Colorado':'CO','Connecticut':'CT','Delaware':'DE','District of Columbia':'DC','Florida':'FL','Georgia':'GA','Hawaii':'HI','Idaho':'ID','Illinois':'IL','Indiana':'IN','Iowa':'IA','Kansas':'KS','Kentucky':'KY','Louisiana':'LA','Maine':'ME','Maryland':'MD','Massachusetts':'MA','Michigan':'MI','Minnesota':'MN','Mississippi':'MS','Missouri':'MO','Montana':'MT','Nebraska':'NE','Nevada':'NV','New Hampshire':'NH','New Jersey':'NJ','New Mexico':'NM','New York':'NY','North Carolina':'NC','North Dakota':'ND','Ohio':'OH','Oklahoma':'OK','Oregon':'OR','Pennsylvania':'PA','Rhode Island':'RI','South Carolina':'SC','South Dakota':'SD','Tennessee':'TN','Texas':'TX','Utah':'UT','Vermont':'VT','Virginia':'VA','Washington':'WA','West Virginia':'WV','Wisconsin':'WI','Wyoming':'WY','Puerto Rico':'PR','U.S. Virgin Islands':'VI'};

  function $(selector){return document.querySelector(selector)}
  function $$(selector){return Array.prototype.slice.call(document.querySelectorAll(selector))}
  function esc(value){return String(value==null?'':value).replace(/[&<>'"]/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]})}
  function norm(value){return String(value||'').toLowerCase().replace(/&/g,'and').replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'')}
  function normSearch(value){return String(value||'').toLowerCase().replace(/&/g,'and').replace(/[^a-z0-9]+/g,' ').replace(/\s+/g,' ').trim()}
  function searchMatch(haystack,query){var q=normSearch(query);if(!q)return true;var hay=' '+normSearch(haystack)+' ';if(hay.indexOf(' '+q+' ')>-1||hay.indexOf(q)>-1)return true;return q.split(' ').filter(Boolean).every(function(part){return hay.indexOf(' '+part+' ')>-1||hay.indexOf(part)>-1})}
  function text(obj){return JSON.stringify(obj||{}).toLowerCase()}
  function uniq(items){return Array.from(new Set(items||[])).filter(Boolean).sort()}
  function safeUrl(url){return url && /^https?:\/\//i.test(url) ? url : ''}
  function plainLink(label,url){var safe=safeUrl(url);return safe?'<a href="'+esc(safe)+'" target="_blank" rel="noopener" onclick="event.stopPropagation()">'+esc(label)+' ↗</a>':esc(label)}
  function debounce(fn,ms){var timer;return function(){var args=arguments,self=this;clearTimeout(timer);timer=setTimeout(function(){fn.apply(self,args)},ms)}}
  function parseDate(str){if(!str)return null;var d=new Date(String(str)+'T00:00:00');return isNaN(d.getTime())?null:d}
  function dateMs(str){var d=parseDate(str);return d?d.getTime():null}
  function inferredStartMs(o){var exact=dateMs(o&&o.startDate);if(exact!=null)return exact;var month=Number((o||{}).month||0);if(month>=1&&month<=12){var year=Number((o||{}).publicCycleYear||(o||{}).eventYear||2026);return new Date(year,month-1,1).getTime()}return Number.MAX_SAFE_INTEGER}
  function fmtShort(d,withYear){if(!d)return '';return SHORT_MONTHS[d.getMonth()]+' '+d.getDate()+(withYear?', '+d.getFullYear():'')}
  function festivalDates(o){var s=parseDate(o.startDate),e=parseDate(o.endDate);if(!s)return '';if(!e||e.getTime()===s.getTime())return fmtShort(s,true);return fmtShort(s,false)+' – '+fmtShort(e,true)}
  function productionWindow(o){var s=parseDate(o.startDate);if(!s)return '';var e=parseDate(o.endDate)||s;var big=(o.departments||[]).length>=9;var build=new Date(s.getTime()-(big?9:4)*86400000);var strike=new Date(e.getTime()+(big?3:2)*86400000);return fmtShort(build,false)+' – '+fmtShort(strike,true)}
  function sortOpportunities(list){return (list||[]).slice().sort(function(a,b){var ad=inferredStartMs(a),bd=inferredStartMs(b);if(ad!==bd)return ad-bd;var d=(b.longTermValueScore||0)-(a.longTermValueScore||0);if(d)return d;return String(a.name||'').localeCompare(String(b.name||''))})}

  function getSchedule(){try{return JSON.parse(localStorage.getItem(SCH_KEY)||'[]')}catch(e){return []}}
  function saveSchedule(ids){try{localStorage.setItem(SCH_KEY,JSON.stringify(ids))}catch(e){}}
  window.addGig=function(id){var s=getSchedule();if(s.indexOf(id)<0)s.push(id);saveSchedule(s);renderPage()};
  window.removeGig=function(id){saveSchedule(getSchedule().filter(function(x){return x!==id}));renderPage()};
  window.clearSchedule=function(){saveSchedule([]);renderPage()};

  function branchName(id){var b=branches.find(function(x){return x.id===id});return b?b.name:id}
  function bestLink(e){var links=e.links||{};return links.apply||links.careers||links.contact||links.directory||links.homepage||''}
  function knownProducer(o){var name=String(((o.producer||{}).name)||'').trim();if(!name)return '';var low=name.toLowerCase();if(low.indexOf('verify')>-1||low==='unknown'||low==='tbd')return '';return name.replace(/\s*[,/]?\s*verify.*$/i,'').replace(/\s*\/\s*partners$/i,'').trim()}
  function producerKey(o){return knownProducer(o)||UNKNOWN_PRODUCER}
  function branchSummary(o,limit){var names=(o.departments||[]).map(branchName);var shown=names.slice(0,limit||4);var extra=Math.max(0,names.length-shown.length);return esc(shown.join(' · ')+(extra?' +'+extra+' more':''))}
  function employerById(id){return employers.find(function(e){return e.id===id})}
  function matchingEmployers(branchId){return employers.filter(function(e){return (e.departments||[]).includes(branchId)})}
  function matchingOpportunities(branchId){return opportunities.filter(function(o){return (o.departments||[]).includes(branchId)})}
  function employerRow(e){var links=e.links||{};var url=links.apply||links.careers||links.contact||links.directory||links.homepage||'';var label=(links.apply||links.careers)?'Apply / careers':'Website / contact';var type=e.type?'<span class="sub" style="font-size:.74rem">'+esc(e.type)+'</span><br>':'';return '<li style="margin:0 0 9px;list-style:none"><b>'+esc(e.name)+'</b><br>'+type+plainLink(label,url)+'</li>'}

  function loadScript(src){return new Promise(function(resolve){if($('script[data-loader="'+src+'"]'))return resolve();var s=document.createElement('script');s.src=src;s.async=false;s.dataset.loader=src;s.onload=resolve;s.onerror=function(){console.warn('Could not load',src);resolve()};document.head.appendChild(s)})}
  function loadBranchManifest(){return loadScript('data/packages/branch-research-manifest.js?v=manifest2').then(function(){if(Array.isArray(window.BRANCH_RESEARCH_MANIFEST)&&window.BRANCH_RESEARCH_MANIFEST.length)branchFiles=window.BRANCH_RESEARCH_MANIFEST.slice()})}
  var _branchResearchPromise=null;
  function ensureBranchResearch(){if(_branchResearchPromise)return _branchResearchPromise;_branchResearchPromise=loadBranchManifest().then(function(){return Promise.all(branchFiles.map(function(file){return loadScript('data/packages/'+file+'?v=manifest2')}))}).then(buildBranchIndex);return _branchResearchPromise}
  function buildBranchIndex(){var records=[];Object.keys(window).forEach(function(key){if(!/^OPPORTUNITY_BRANCH_RESEARCH_BATCH_/.test(key))return;var ds=window[key];if(!ds||!Array.isArray(ds.targets))return;ds.targets.forEach(function(t){records.push(Object.assign({},t,{branchId:t.branchId||ds.branchId,branchName:t.branchName||ds.branchName,batchId:ds.batchId,datasetKey:key}))})});branchIndex={records:records,byKey:{}};records.forEach(function(r){branchIndex.byKey[norm(r.opportunityId)+'::'+r.branchId]=r;branchIndex.byKey[norm(r.opportunityName)+'::'+r.branchId]=r});branchDataReady=true}
  function classify(o){var hasSource=!!o.active2026SourceUrl;var copy=Object.assign({},o);copy.intelligence=Object.assign({publicSources:hasSource?[{label:'active status source',url:o.active2026SourceUrl}]:[]},o.intelligence||{});return copy}
  function findBranchRecord(o,branchId){return branchIndex.byKey[norm(o.id)+'::'+branchId]||branchIndex.byKey[norm(o.name)+'::'+branchId]||null}

  function filterValues(){return {q:(($('#q')||{}).value||'').trim().toLowerCase(),branch:(($('#branchFilter')||{}).value||''),region:(($('#regionFilter')||{}).value||''),month:(($('#monthFilter')||{}).value||''),year:(($('#yearFilter')||{}).value||''),type:(($('#employerTypeFilter')||{}).value||''),state:(($('#stateFilter')||{}).value||''),producer:(($('#producerFilter')||{}).value||''),festival:(($('#festivalFilter')||{}).value||''),employer:(($('#employerFilter')||{}).value||'')}}
  var SAVED_KEY='atlas_saved_opportunities';
  function getSavedIds(){try{var raw=JSON.parse(localStorage.getItem(SAVED_KEY)||'[]');return Array.isArray(raw)?raw:[]}catch(e){return []}}
  function isSaved(id){return getSavedIds().indexOf(id)>-1}
  function setSavedIds(arr){try{localStorage.setItem(SAVED_KEY,JSON.stringify(arr))}catch(e){}}
  window.toggleOppSave=function(id,evt){if(evt&&evt.stopPropagation)evt.stopPropagation();var arr=getSavedIds();var i=arr.indexOf(id);if(i>-1)arr.splice(i,1);else arr.push(id);setSavedIds(arr);renderPage()};
  function savedOnlyActive(){var b=$('#savedOnly');return !!(b&&b.getAttribute('aria-pressed')==='true')}
  window.toggleSavedOnly=function(){var b=$('#savedOnly');if(!b)return;var active=b.getAttribute('aria-pressed')==='true';b.setAttribute('aria-pressed',String(!active));b.classList.toggle('active',!active);b.textContent=(!active?'★ Saved':'☆ Saved');renderPage()};
  function oppHay(o){return String([o.name,o.city,o.state,o.venue,knownProducer(o),(o.departments||[]).map(branchName).join(' ')].join(' ')).toLowerCase()}
  function activeOpportunities(){var f=filterValues();var savedOnly=savedOnlyActive();var now=new Date();var todayMs=new Date(now.getFullYear(),now.getMonth(),now.getDate()).getTime();return sortOpportunities(opportunities.filter(function(o){var monthMatch=!f.month||String(o.month)===f.month;if(!monthMatch)return false;var oppYear=String(o.eventYear||o.publicCycleYear||2026);var yearMatch=!f.year||String(oppYear)===f.year;if(!yearMatch)return false;var endDate=parseDate(o.endDate)||parseDate(o.startDate);var isFuture=endDate&&endDate.getTime()>=todayMs;return (!f.q||searchMatch(oppHay(o),f.q))&&(!f.branch||(o.departments||[]).includes(f.branch))&&(!f.region||o.region===f.region)&&isFuture&&(!f.state||o.state===f.state)&&(!f.producer||producerKey(o)===f.producer)&&(!savedOnly||isSaved(o.id))}))}
  function activeLocals(){var f=filterValues();return iatseLocals.filter(function(l){return (!f.q||searchMatch(iatseText(l),f.q))&&(!f.region||String(l.jurisdiction||'').includes(f.region)||String(l.states||'').includes(f.region)||l.district===f.region)})}

  function fillFilters(){if(!$('#filters'))return;var branchSelect=$('#branchFilter');if(branchSelect&&branchSelect.options.length<=1)branches.forEach(function(b){branchSelect.innerHTML+='<option value="'+esc(b.id)+'">'+esc(b.name)+'</option>'});var monthSelect=$('#monthFilter');if(monthSelect&&monthSelect.options.length<=1)MONTHS.forEach(function(m,i){monthSelect.innerHTML+='<option value="'+(i+1)+'">'+m+'</option>'});var yearSelect=$('#yearFilter');if(yearSelect&&yearSelect.options.length<=1){var years=uniq(opportunities.map(function(o){return String(o.eventYear||o.publicCycleYear||2026)})).sort();years.forEach(function(y){yearSelect.innerHTML+='<option value="'+esc(y)+'">'+esc(y)+'</option>'});if(!yearSelect.value)yearSelect.value='2026'}var stateSelect=$('#stateFilter');if(stateSelect&&stateSelect.options.length<=1)uniq(opportunities.filter(function(o){return o.state&&o.state!=='US'}).map(function(o){return o.state})).forEach(function(st){stateSelect.innerHTML+='<option value="'+esc(st)+'">'+esc(st)+'</option>'});var typeSelect=$('#employerTypeFilter');if(typeSelect&&typeSelect.options.length<=1)uniq(employers.map(function(e){return e.type})).forEach(function(t){typeSelect.innerHTML+='<option>'+esc(t)+'</option>'});var regionSelect=$('#regionFilter');if(regionSelect&&regionSelect.options.length<=1)uniq(opportunities.map(function(o){return o.region}).concat(employers.map(function(e){return e.region}))).forEach(function(r){regionSelect.innerHTML+='<option>'+esc(r)+'</option>'});var producerSelect=$('#producerFilter');if(producerSelect&&producerSelect.options.length<=1){var labels=uniq(opportunities.map(producerKey)).sort(function(a,b){if(a===UNKNOWN_PRODUCER)return 1;if(b===UNKNOWN_PRODUCER)return -1;return a.localeCompare(b)});producerSelect.innerHTML='<option value="">All promoters</option>'+labels.map(function(p){return '<option value="'+esc(p)+'">'+esc(p===UNKNOWN_PRODUCER?'Unknown promoter':p)+'</option>'}).join('')}var debounced=debounce(renderPage,150);$$('#filters input,#filters select').forEach(function(input){if(input.dataset.coreBound)return;input.dataset.coreBound='true';input.addEventListener('input',input.tagName==='SELECT'?renderPage:debounced);input.addEventListener('change',renderPage)});var reset=$('#reset');if(reset&&!reset.dataset.coreBound){reset.dataset.coreBound='true';reset.onclick=function(){$$('#filters input,#filters select').forEach(function(input){input.value=''});var yf=$('#yearFilter');if(yf)yf.value='2026';var savedBtn=$('#savedOnly');if(savedBtn){savedBtn.setAttribute('aria-pressed','false');savedBtn.classList.remove('active');savedBtn.textContent='☆ Saved'}renderPage()}}}
  function applyUrlFilters(){if(!$('#filters'))return;var p=new URLSearchParams(window.location.search||'');[['q','#q'],['branch','#branchFilter'],['region','#regionFilter'],['month','#monthFilter'],['year','#yearFilter'],['state','#stateFilter'],['producer','#producerFilter'],['type','#employerTypeFilter'],['festival','#festivalFilter'],['employer','#employerFilter']].forEach(function(pair){var val=p.get(pair[0]);var input=$(pair[1]);if(val!=null&&input)input.value=val})}

  function opportunityCard(o){var venue=String(o.venue||'').trim();var hasVenue=venue&&venue.toLowerCase().indexOf('verify')===-1&&venue.toLowerCase()!=='unknown'&&venue.toLowerCase()!=='tbd';var dates=festivalDates(o);var prodWindow=productionWindow(o);var producer=knownProducer(o);var saved=isSaved(o.id);return '<article class="card click oppcard" role="button" tabindex="0" data-keyclick onclick="openOpportunity(\''+esc(o.id)+'\')"><button type="button" class="save-star" aria-pressed="'+(saved?'true':'false')+'" aria-label="'+(saved?'Remove '+esc(o.name)+' from saved':'Save '+esc(o.name))+'" onclick="toggleOppSave(\''+esc(o.id)+'\',event)">'+(saved?'★':'☆')+'</button><h3>'+esc(o.name)+'</h3><div class="sub">'+esc(o.city)+', '+esc(o.state)+(hasVenue?' • '+esc(venue):'')+'</div>'+(dates?'<p class="oppline"><b>Festival dates:</b> '+esc(dates)+'</p>':'')+(prodWindow?'<p class="oppline"><b>Approx. production window:</b> '+esc(prodWindow)+'</p>':'')+(producer?'<p class="oppline"><b>Producer / promoter:</b> '+esc(producer)+'</p>':'')+'<p class="oppline"><b>Branches:</b> '+branchSummary(o,4)+'</p><p class="oppline cardcta">Open public employer contacts →</p></article>'}
  function iatseFamily(l){var t=String([l.district,l.jurisdiction,l.craft].join(' ')).toLowerCase();if(l.district==='National')return 'National local / guild';if(t.indexOf('broadcast')>-1||t.indexOf('television engineers')>-1)return 'Broadcast';if(t.indexOf('studio')>-1||t.indexOf('motion picture')>-1||t.indexOf('cinematographer')>-1||t.indexOf('editor')>-1||t.indexOf('script')>-1||t.indexOf('production coordinator')>-1||t.indexOf('grips')>-1||t.indexOf('sound')>-1||t.indexOf('art director')>-1)return 'Motion picture / TV';if(t.indexOf('exhibition')>-1||t.indexOf('display')>-1||t.indexOf('bill posters')>-1||t.indexOf('arena employees')>-1)return 'Tradeshow / exhibition';if(t.indexOf('wardrobe')>-1||t.indexOf('make-up')>-1||t.indexOf('hairstylist')>-1||t.indexOf('costume')>-1)return 'Wardrobe / makeup / costume';if(t.indexOf('ticket')>-1||t.indexOf('treasurer')>-1||t.indexOf('front of house')>-1||t.indexOf('box office')>-1||t.indexOf('casino')>-1||t.indexOf('theater employees')>-1)return 'Box office / venue ops';if(t.indexOf('stage')>-1||t.indexOf('mixed')>-1)return 'Stagecraft / live events';return 'Other / verify craft'}
  function iatseStateAliases(l){var aliases=[];(l.states||[]).forEach(function(s){if(STATE_ABBR[s])aliases.push(STATE_ABBR[s]);});return aliases.join(' ')}
  function iatseText(l){var local=String(l.local||'');var jurisdiction=String(l.jurisdiction||'');return String([local,'local '+local,'iatse '+local,'iatse local '+local,'local #'+local,'#'+local,local+' '+jurisdiction,'local '+local+' '+jurisdiction,'iatse local '+local+' '+jurisdiction,l.district,jurisdiction,l.craft,(l.states||[]).join(' '),iatseStateAliases(l),iatseFamily(l)].join(' ')).toLowerCase()}
  function iatseCard(l){var states=(l.states||[]).join(', ');var family=iatseFamily(l);return '<article class="card click" role="button" tabindex="0" data-keyclick data-local="'+esc(l.local)+'" data-district="'+esc(l.district)+'" onclick="openLocalEl(this)"><h3>IATSE Local '+esc(l.local)+'</h3><div class="sub">'+esc(l.district)+' • '+esc(l.jurisdiction)+'</div><p><b>Directory description:</b> '+esc(l.craft||'Multiple crafts — verify with the official source')+'</p><p><b>Derived family:</b> '+esc(family)+'</p>'+(states?'<p><b>Snapshot area:</b> '+esc(states)+'</p>':'')+'<p class="cardcta">Open local research context →</p></article>'}
  function stat(label,value){return '<div class="stat"><b>'+esc(value)+'</b><span>'+esc(label)+'</span></div>'}
  function countBy(items,fn){return items.reduce(function(a,x){var k=fn(x)||'Unknown';a[k]=(a[k]||0)+1;return a},{})}
  function entries(obj){return Object.entries(obj).sort(function(a,b){return b[1]-a[1]||a[0].localeCompare(b[0])})}

  function renderHome(){var el=$('#app');if(!el)return;var now=new Date();var todayMs=new Date(now.getFullYear(),now.getMonth(),now.getDate()).getTime();var upcomingAll=opportunities.filter(function(o){var endDate=parseDate(o.endDate)||parseDate(o.startDate);return endDate&&endDate.getTime()>=todayMs});var cycleCount=opportunities.filter(function(o){return Number(o.eventYear||o.publicCycleYear||2026)===2026}).length;var upcoming=upcomingAll.slice().sort(function(a,b){return inferredStartMs(a)-inferredStartMs(b)}).slice(0,6);el.innerHTML='<section class="card guide-home-callout" style="margin:0 0 20px"><h2 style="margin:0 0 8px">Guide</h2><p class="lead" style="margin:0 0 14px">Use this first: it explains how to read the atlas, compare public festival opportunities, use sources, and turn the map into a work-research plan.</p><a class="btn" href="guide.html">Open Guide →</a></section><div class="pathway-grid"><a class="pathway" href="opportunities.html"><h4>Opportunities</h4><p class="pathway-skills">Festival profiles with dates, locations, and public employer contacts.</p><span class="pathway-count">'+cycleCount+' festivals →</span></a><a class="pathway" href="employers.html"><h4>Employers</h4><p class="pathway-skills">Public apply, careers, and contact links.</p><span class="pathway-count">'+employers.length+' employers →</span></a><a class="pathway" href="calendar.html"><h4>Calendar</h4><p class="pathway-skills">Month-by-month planning.</p><span class="pathway-count">Browse →</span></a><a class="pathway" href="iatse.html"><h4>IATSE pathways</h4><p class="pathway-skills">Understand locals, referral routes, membership, training, and touring as separate layers.</p><span class="pathway-count">Research routes →</span></a></div><h3 style="margin:22px 0 10px;font-size:1.1rem">Upcoming festivals</h3>'+(upcoming.length?'<div style="display:grid;gap:7px">'+upcoming.map(function(o){var dates=festivalDates(o);return '<div class="card click" role="button" tabindex="0" data-keyclick style="padding:12px 16px;display:flex;align-items:center;gap:12px" onclick="openOpportunity(\''+esc(o.id)+'\')"><div style="flex:1;min-width:0"><div style="font-weight:800;color:#fff;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">'+esc(o.name)+'</div><div class="sub" style="font-size:.8rem;margin-top:2px">'+esc(o.city)+', '+esc(o.state)+(dates?' · '+esc(dates):'')+'</div></div><span style="color:#ffd66b;font-size:.8rem;white-space:nowrap;flex-shrink:0">Open →</span></div>'}).join('')+'</div>':'<p class="sub">No upcoming festivals in the current data.</p>')+'<div class="home-links" style="margin-top:14px"><a href="opportunities.html" class="btn">All festivals</a><a href="iatse.html" class="btn">IATSE organizations</a><a href="map.html" class="btn">Map</a><a href="contribute.html" class="btn">Contribute</a></div>'}
  var PER_PAGE=10;
  function pageList(page,pages){var c=page+1,a=[],i;for(i=1;i<=pages;i++){if(i===1||i===pages||(i>=c-1&&i<=c+1))a.push(i);else if(a[a.length-1]!=='…')a.push('…');}return a;}
  function pgControls(page,pages,total,per,fn){if(pages<=1)return '';var start=page*per,end=Math.min(total,start+per);var nums=pageList(page,pages).map(function(p){if(p==='…')return '<span class="pager-gap">…</span>';var idx=p-1;return '<button class="btn pager-num'+(idx===page?' active':'')+'" type="button"'+(idx===page?' aria-current="page"':'')+' onclick="'+fn+'('+idx+')" aria-label="Page '+p+'">'+p+'</button>'}).join('');return '<div class="pager"><div class="pager-row"><button class="btn" type="button"'+(page<=0?' disabled':'')+' onclick="'+fn+'('+(page-1)+')" aria-label="Previous results">‹ Prev</button><div class="pager-nums">'+nums+'</div><button class="btn" type="button"'+(page>=pages-1?' disabled':'')+' onclick="'+fn+'('+(page+1)+')" aria-label="Next results">Next ›</button></div><span class="pager-info">Showing '+(start+1)+'–'+end+' of '+total+' · Page '+(page+1)+' of '+pages+'</span></div>'}
  var oppPage=0,oppSig='__init';
  function renderOpportunities(){var el=$('#app');if(!el)return;var data=activeOpportunities();var savedOnly=savedOnlyActive();var sig=JSON.stringify(filterValues())+'|'+savedOnly;if(sig!==oppSig){oppSig=sig;oppPage=0;}var pages=Math.max(1,Math.ceil(data.length/PER_PAGE));if(oppPage>=pages)oppPage=pages-1;if(oppPage<0)oppPage=0;var pageData=data.slice(oppPage*PER_PAGE,oppPage*PER_PAGE+PER_PAGE);var stateCount=uniq(data.filter(function(o){return o.state&&o.state!=='US'}).map(function(o){return o.state})).length;var pg=pgControls(oppPage,pages,data.length,PER_PAGE,'oppPageSet');var savedBtn=$('#savedOnly');if(savedBtn){var savedCount=getSavedIds().length;savedBtn.textContent=(savedOnly?'★':'☆')+' Saved'+(savedCount?' ('+savedCount+')':'')}var emptyMsg=savedOnly?'<p>No saved festivals yet. Tap the ☆ on any festival card to save it here.</p>':'<p>No festivals match the current filter.</p>';el.innerHTML='<h2>Festivals</h2><p class="lead">Browse festivals by date, city, venue, producer, production window, and public employer contacts.</p><div class="stats" style="grid-template-columns:repeat(2,1fr);margin:0 0 18px"><div class="stat"><b>'+data.length+'</b><span>festivals</span></div><div class="stat"><b>'+stateCount+'</b><span>states</span></div></div><div class="notice">Know an active festival that belongs here? Submit it on the <a href="contribute.html">Contribute page</a>.</div>'+pg+'<div class="grid">'+(data.length?pageData.map(opportunityCard).join(''):emptyMsg)+'</div>'+pg}
  window.oppPageSet=function(n){oppPage=Number(n)||0;renderOpportunities();var el=$('#app');if(el&&el.scrollIntoView)try{el.scrollIntoView({behavior:'smooth',block:'start'})}catch(e){}};
  var iatseTab='join',iatseLocPage=0,iatseLocSig='__init';
  window.iatseSetTab=function(t){iatseTab=t;if(t!=='find'){var qi=$('#q');if(qi)qi.value='';}renderIatse();var el=$('#app');if(el&&el.scrollIntoView)try{el.scrollIntoView({behavior:'smooth',block:'start'})}catch(e){}};
  window.iatseLocPageSet=function(n){iatseLocPage=Number(n)||0;renderIatse();var el=$('#app');if(el&&el.scrollIntoView)try{el.scrollIntoView({behavior:'smooth',block:'start'})}catch(e){}};
  function iatseTabBtn(id,label,active){return '<button class="iatse-tab'+(active===id?' active':'')+'" type="button" id="iatse-tab-'+id+'" role="tab" aria-selected="'+(active===id?'true':'false')+'" aria-controls="iatse-panel-'+id+'" tabindex="'+(active===id?'0':'-1')+'" onclick="iatseSetTab(\''+id+'\')">'+label+'</button>'}
  var IATSE_TAB_ORDER=['join','find','about'];
  document.addEventListener('keydown',function(e){
    if(e.key!=='ArrowRight'&&e.key!=='ArrowLeft')return;
    if(!e.target||!e.target.closest||!e.target.closest('.iatse-tabs'))return;
    var current=IATSE_TAB_ORDER.indexOf((e.target.id||'').replace('iatse-tab-',''));
    if(current<0)return;
    e.preventDefault();
    var next=(current+(e.key==='ArrowRight'?1:IATSE_TAB_ORDER.length-1))%IATSE_TAB_ORDER.length;
    window.iatseSetTab(IATSE_TAB_ORDER[next]);
    var btn=document.getElementById('iatse-tab-'+IATSE_TAB_ORDER[next]);
    if(btn)btn.focus();
  });
  function renderIatse(){
    var el=$('#app');if(!el)return;
    var info=window.IATSE_ORGANIZATION_INFO||{};var summary=info.officialSummary||{};var families=info.departmentFamilies||[];var layers=info.institutionLayers||[];var routes=info.entryRoutePatterns||[];var nationalRoutes=info.nationalRoutes||[];var training=info.training||{};var touring=info.touring||{};var directoryMeta=((window.IATSE_US_LOCAL_DIRECTORY||{}).meta)||{};
    var q=(filterValues().q||'').trim().toLowerCase();
    var all=iatseLocals.slice();
    var matched=q?all.filter(function(l){return searchMatch(iatseText(l),q)}):all;
    var primary=[],secondary=[];matched.forEach(function(l){if(l.district==='National')secondary.push(l);else primary.push(l)});
    if(q!==iatseLocSig){iatseLocSig=q;iatseLocPage=0;}
    var tab=q?'find':iatseTab;
    var join='<p class="lead">There is no single national sequence from first contact to work or membership. IATSE identifies two broad paths—follow an existing local’s criteria, or organize a workplace—but each local defines its own current processes.</p>'+
      '<div class="steps">'+
        '<div class="step-card"><span class="step-n">1</span><h4>Identify craft and geography</h4><p>Local numbers alone do not establish craft or jurisdiction. Start with the kind of work and the market where it occurs.</p></div>'+
        '<div class="step-card"><span class="step-n">2</span><h4>Check the current official directory</h4><p>Use <b>Find a local</b> as an initial index, then use the official directory linked on the Sources page. The Atlas list is a partial dated snapshot.</p></div>'+
        '<div class="step-card"><span class="step-n">3</span><h4>Separate the routes</h4><p>Read the organization’s current work or referral route, membership route, apprenticeship or training route, and workplace-organizing route separately.</p></div>'+
        '<div class="step-card"><span class="step-n">4</span><h4>Verify current requirements</h4><p>Intake windows, availability rules, skill evidence, training, costs, and membership criteria vary and can change with market need.</p></div>'+
        '<div class="step-card"><span class="step-n">5</span><h4>Keep outcomes distinct</h4><p>Registration is not referral; referral is not guaranteed work; membership is not employment; and the local may not be the employer of record.</p></div>'+
      '</div>'+
      '<h3 class="section-kicker">National-level routes</h3><div class="grid">'+nationalRoutes.map(function(r){return '<article class="card"><h3>'+esc(r.label)+'</h3><p>'+esc(r.summary)+'</p>'+(r.boundary?'<p class="sub">'+esc(r.boundary)+'</p>':'')+'</article>'}).join('')+'</div>'+
      '<h3 class="section-kicker">Locally observed route types</h3><div class="grid">'+routes.map(function(r){return '<article class="card"><h3>'+esc(r.label)+'</h3><p>'+esc(r.summary)+'</p></article>'}).join('')+'</div>'+
      '<h3 class="section-kicker">Training and credentials</h3><div class="card"><p>'+esc(training.summary||'Training and credential programs vary by provider and eligibility.')+'</p><p><b>Keep separate:</b> '+esc(training.distinction||'Availability does not prove a requirement.')+'</p><p class="sub">'+esc(training.universalCredentialRule||'Verify any requirement for the specific local, employer, venue, classification, or call.')+'</p></div>'+
      '<h3 class="section-kicker">Touring</h3><div class="card"><p>'+esc(touring.summary||'Touring agreements and labor coordination are separate from local referral routes.')+'</p>'+((touring.boundaries||[]).length?'<ul>'+(touring.boundaries||[]).map(function(item){return '<li>'+esc(item)+'</li>'}).join('')+'</ul>':'')+'</div>'+
      '<div class="home-links" style="margin-top:18px"><a class="btn" href="sources.html#iatse-sources">Open official IATSE sources →</a><a class="btn" href="contribute.html">Submit a public route correction →</a></div>';
    var pages=Math.max(1,Math.ceil(primary.length/PER_PAGE));if(iatseLocPage>=pages)iatseLocPage=pages-1;if(iatseLocPage<0)iatseLocPage=0;
    var pageLocals=primary.slice(iatseLocPage*PER_PAGE,iatseLocPage*PER_PAGE+PER_PAGE);
    var locPg=pgControls(iatseLocPage,pages,primary.length,PER_PAGE,'iatseLocPageSet');
    var find='<p class="lead">Search this '+all.length+'-record partial snapshot by local number, city, state, state abbreviation, craft, district, or organization family. Use the current official directory on the <a href="sources.html#iatse-sources">Sources page</a> before relying on a result.</p>'+
      '<div class="notice"><b>Coverage warning:</b> this retained 2026-06-21 snapshot is not a complete U.S. directory. The 2026-09-07 audit found omissions in at least Delaware, Idaho, New Hampshire, North Dakota, and South Dakota. A missing result does not mean no local exists.</div>'+
      (q?'<div class="notice">Showing '+matched.length+' result'+(matched.length===1?'':'s')+' for <b>'+esc(q)+'</b>.</div>':'')+
      (primary.length?locPg+'<div class="grid">'+pageLocals.map(iatseCard).join('')+'</div>'+locPg:'')+
      (secondary.length?'<h3 class="section-kicker">National / regional / craft organizations</h3><div class="grid">'+secondary.map(iatseCard).join('')+'</div>':'')+
      (!matched.length?'<p class="sub">No IATSE organizations match your search. Try a local number, city, state, state abbreviation, or craft.</p>':'');
    var districtCards=entries(countBy(all,function(l){return l.district})).map(function(p){return '<article class="card"><h3>'+esc(p[0])+'</h3><p class="sub">'+p[1]+' listed organization'+(p[1]===1?'':'s')+'</p></article>'}).join('');
    var craftCards=entries(countBy(all,iatseFamily)).map(function(p){return '<article class="card"><h3>'+esc(p[0])+'</h3><p class="sub">'+p[1]+' listed organization'+(p[1]===1?'':'s')+'</p></article>'}).join('');
    var about='<div class="grid">'+
        '<article class="card"><h3>IATSE International</h3><p class="sub">'+esc(summary.fullName||'International Alliance of Theatrical Stage Employees')+'</p><p>Founded: '+esc(summary.founded||'1893')+'. Public scope: '+esc(summary.workerCountPublicClaim||'more than 170,000 entertainment professionals')+'.</p>'+(summary.workerCountSourceMismatch?'<p class="sub">'+esc(summary.workerCountSourceMismatch)+'</p>':'')+'</article>'+
        '<article class="card"><h3>Autonomous local unions</h3><p class="sub">Geographic and craft jurisdiction</p><p>'+esc(summary.localUnionPublicClaim||'More than 360 local unions in the United States and Canada')+'. Each local defines its own internal rules and routes.</p></article>'+
        '<article class="card"><h3>Not one employer</h3><p class="sub">Labor organization, referral, and employment are different</p><p>A local may operate a referral mechanism that connects workers to one or more employers. Resolve the actual employer and agreement from specific evidence.</p></article>'+
      '</div>'+
      '<h3 class="section-kicker">Institutional layers</h3><div class="grid">'+layers.map(function(layer){return '<article class="card"><h3>'+esc(layer.label)+'</h3><p>'+esc(layer.role)+'</p><p class="sub">'+esc(layer.boundary)+'</p></article>'}).join('')+'</div>'+
      '<div class="stats" style="grid-template-columns:repeat(4,1fr);margin:18px 0">'+stat('snapshot records',all.length)+stat('district / national labels',uniq(all.map(function(l){return l.district})).length)+stat('mapped states / territories',uniq(all.flatMap(function(l){return l.states||[]})).length)+stat('derived craft families',uniq(all.map(iatseFamily)).length)+'</div>'+
      (families.length?'<h3 class="section-kicker">Organization families</h3><div class="grid">'+families.map(function(f){return '<article class="card"><h3>'+esc(f.label)+'</h3><p>'+esc(f.relevance)+'</p><p class="sub">Search terms: '+esc((f.searchTerms||[]).join(', '))+'</p></article>'}).join('')+'</div>':'')+
      '<h3 class="section-kicker">District / organization coverage</h3><div class="grid">'+districtCards+'</div>'+
      '<h3 class="section-kicker">Craft-family coverage</h3><div class="grid">'+craftCards+'</div>';
    el.innerHTML=
      '<p class="lead">'+esc(summary.workerScope||'IATSE spans live performance, broadcast, motion picture and television, and trade-show work.')+' Production Atlas treats it as a labor-organization ecosystem with distinct institutional, local, referral, employer, membership, training, and touring layers.</p>'+
      '<div class="notice"><b>Independent public research:</b> this page is not a job referral, membership decision, legal or jurisdiction ruling, or event-specific labor assignment. Production Atlas is not affiliated with or endorsed by IATSE or any local union.</div>'+
      '<div class="iatse-tabs" role="tablist">'+iatseTabBtn('join','Work &amp; membership routes',tab)+iatseTabBtn('find','Find a local',tab)+iatseTabBtn('about','How IATSE is structured',tab)+'</div>'+
      '<div class="iatse-panel" id="iatse-panel-join" role="tabpanel" aria-labelledby="iatse-tab-join" data-tab="join"'+(tab==='join'?'':' hidden')+'>'+join+'</div>'+
      '<div class="iatse-panel" id="iatse-panel-find" role="tabpanel" aria-labelledby="iatse-tab-find" data-tab="find"'+(tab==='find'?'':' hidden')+'>'+find+'</div>'+
      '<div class="iatse-panel" id="iatse-panel-about" role="tabpanel" aria-labelledby="iatse-tab-about" data-tab="about"'+(tab==='about'?'':' hidden')+'>'+about+'</div>';
  }
  function renderMatrix(){var el=$('#app');if(!el)return;var rows=branches.map(function(b){var links=matchingEmployers(b.id).slice(0,10).map(function(e){return plainLink(e.name,bestLink(e))}).join('<br>');return '<tr><td><b>'+esc(b.name)+'</b><br><span class="sub">'+esc(b.question||'')+'</span></td><td>'+esc((b.researchNeeds||[]).join(', '))+'</td><td>'+esc((b.workerFocus||[]).join(', '))+'</td><td>'+links+'</td><td><button class="btn" onclick="openBranch(\''+esc(b.id)+'\')">Open branch</button></td></tr>'}).join('');el.innerHTML='<h2>Employer and Hiring Matrix</h2><div class="tablewrap"><table class="matrix"><thead><tr><th>Branch</th><th>Research needs</th><th>Worker focus</th><th>General leads</th><th>Detail</th></tr></thead><tbody>'+rows+'</tbody></table></div>'}
  function renderBranches(){var el=$('#app');if(!el)return;el.innerHTML='<h2>Production Branches</h2><p class="lead">Employers and companies organized by production branch.</p><div class="grid">'+branches.map(function(b){var roles=(b.workerFocus||[]).slice(0,4).join(' · ');return '<article class="card click" role="button" tabindex="0" data-keyclick onclick="openBranch(\''+esc(b.id)+'\')"><h3>'+esc(b.name)+'</h3>'+(roles?'<p class="sub">'+esc(roles)+'</p>':'')+'<p><b>Active festivals:</b> '+matchingOpportunities(b.id).length+'</p><p><b>Employers:</b> '+matchingEmployers(b.id).length+'</p></article>'}).join('')+'</div>'}
  function renderAnalytics(){var el=$('#app');if(!el)return;el.innerHTML='<h2>Festival Analytics</h2><p class="lead">Where and when festival production work clusters.</p><div class="stats" style="margin:0 0 20px"><div class="stat"><b>'+opportunities.length+'</b><span>active festivals</span></div><div class="stat"><b>'+uniq(opportunities.map(function(o){return o.state}).filter(function(s){return s&&s!=='US'})).length+'</b><span>states</span></div><div class="stat"><b>'+employers.length+'</b><span>employers</span></div></div>'}
  function renderSources(){var el=$('#app');if(!el)return;var f=filterValues();var rows=[];opportunities.forEach(function(o){((o.intelligence||{}).publicSources||[]).forEach(function(s){if(s.url)rows.push({type:'opportunity',item:o.name,label:s.label||'source',url:s.url,branchIds:o.departments||[]})})});branchIndex.records.forEach(function(r){(r.sourceLinks||[]).forEach(function(s){if(s.url)rows.push({type:'branch',item:r.opportunityName||r.opportunityId,section:r.branchName||r.branchId,label:s.label||'source',url:s.url,branchIds:[r.branchId]})})});var fs=$('#festivalFilter');if(fs){var cur=fs.value;var names=uniq(rows.filter(function(r){return r.type==='opportunity'}).map(function(r){return r.item}));fs.innerHTML='<option value="">All festivals</option>'+names.map(function(n){return '<option value="'+esc(n)+'">'+esc(n)+'</option>'}).join('');if(names.indexOf(cur)>-1)fs.value=cur}var filtered=rows.filter(function(r){return (!f.festival||r.item===f.festival)&&(!f.branch||r.branchIds.indexOf(f.branch)>-1)});el.innerHTML='<h2>Sources</h2><p class="lead">Organized public source list. Sources stay here instead of inside popups.</p><div class="notice">Have a public source link that belongs here? Submit it on the <a href="contribute.html">Contribute page</a>.</div>'+(branchDataReady?'':'<p class="sub">Loading department source records…</p>')+'<div class="stats" style="grid-template-columns:repeat(3,1fr);margin:0 0 18px">'+stat('sources shown',filtered.length)+stat('festival',filtered.filter(function(r){return r.type==='opportunity'}).length)+stat('department',filtered.filter(function(r){return r.type==='branch'}).length)+'</div><div class="tablewrap"><table class="matrix"><thead><tr><th>Festival</th><th>Department</th><th>Source label</th><th>Link</th></tr></thead><tbody>'+(filtered.length?filtered.map(function(r){return '<tr><td>'+esc(r.item)+'</td><td>'+esc(r.type==='branch'?r.section:'')+'</td><td>'+esc(r.label)+'</td><td>'+plainLink('Open source',r.url)+'</td></tr>'}).join(''):'<tr><td colspan="4" style="color:var(--muted)">No sources match the current filter.</td></tr>')+'</tbody></table></div>'}
  function renderSchedule(){var el=$('#app');if(!el)return;var schedule=getSchedule();var scheduled=schedule.map(function(id){return opportunities.find(function(o){return o.id===id})}).filter(Boolean);var browse=activeOpportunities();el.innerHTML='<h2>My Work Schedule</h2><p class="lead">Build your personal browser-local plan. Use filters above to narrow the browse list.</p><div class="stats" style="grid-template-columns:repeat(3,1fr)">'+stat('events planned',scheduled.length)+stat('months covered',uniq(scheduled.map(function(o){return o.month})).length)+stat('regions covered',uniq(scheduled.map(function(o){return o.region})).length)+'</div>'+(scheduled.length?'<button class="btn" style="margin:0 0 14px" onclick="clearSchedule()">Clear all</button><div class="grid">'+scheduled.map(function(o){return '<article class="card"><h3>'+esc(o.name)+'</h3><p class="sub">'+esc(o.city)+', '+esc(o.state)+' · '+esc(festivalDates(o))+'</p><button class="btn" onclick="removeGig(\''+esc(o.id)+'\')">Remove</button></article>'}).join('')+'</div>':'<div class="sched-empty">No events in your schedule yet.</div>')+'<h2 style="margin-top:28px">Browse &amp; Add Events</h2><div class="grid">'+browse.map(function(o){var inSched=schedule.indexOf(o.id)>-1;return '<article class="card"><h3>'+esc(o.name)+'</h3><div class="sub">'+esc(o.city)+', '+esc(o.state)+'</div><p class="oppline"><b>Festival dates:</b> '+esc(festivalDates(o))+'</p><p class="oppline"><b>Branches:</b> '+branchSummary(o,3)+'</p><button class="btn '+(inSched?'sched-in':'sched-add')+'" onclick="'+(inSched?'removeGig':'addGig')+'(\''+esc(o.id)+'\')">'+(inSched?'✓ In schedule':'+ Add to schedule')+'</button></article>'}).join('')+'</div>'}

  function branchCard(o,branchId){var b=branches.find(function(x){return x.id===branchId})||{name:branchId};var r=findBranchRecord(o,branchId);var ids=[],label='Industry companies in this branch';if(r&&(r.confirmedVendors||[]).length){ids=r.confirmedVendors.slice();label='Companies tied to this branch'}else if(r&&(r.publicLeads||[]).length){ids=r.publicLeads.slice();label='Public companies'}else ids=matchingEmployers(branchId).map(function(e){return e.id});var rows=ids.map(employerById).filter(Boolean).slice(0,8).map(employerRow).join('');return '<div class="branch"><h4>'+esc((r&&r.branchName)||b.name)+'</h4>'+(rows?'<p class="sub" style="margin:0 0 8px">'+esc(label)+'</p><ul style="margin:0;padding:0">'+rows+'</ul>':'<p class="sub">No public company listed yet.</p>')+'</div>'}
  window.openOpportunity=function(id){var o=opportunities.find(function(x){return x.id===id});if(!o)return;ensureBranchResearch().then(function(){renderOpportunityModal(o)})};
  function renderOpportunityModal(o){var hasSource=!!o.active2026SourceUrl;var venue=String(o.venue||'').trim();var hasVenue=venue&&venue.toLowerCase().indexOf('verify')===-1&&venue.toLowerCase()!=='unknown'&&venue.toLowerCase()!=='tbd';var dates=festivalDates(o);var prod=productionWindow(o);var producer=knownProducer(o);var branchHtml=(o.departments||[]).map(function(dep){return branchCard(o,dep)}).join('');openModal('<h2 style="margin:0 0 6px">'+esc(o.name)+'</h2><p class="sub">'+esc(o.city)+', '+esc(o.state)+(hasVenue?' • '+esc(venue):'')+'</p><div class="modalgrid">'+(dates?'<div class="detail"><b>Festival dates</b><br>'+esc(dates)+'</div>':'')+(prod?'<div class="detail"><b>Approx. build / strike window</b><br>'+esc(prod)+' (approximate)</div>':'')+(producer?'<div class="detail"><b>Producer / promoter</b><br>'+esc(producer)+'</div>':'')+(hasSource?'<div class="detail"><b>Public source</b><br>Listed on the <a href="sources.html" onclick="event.stopPropagation()">Sources page ↗</a></div>':'')+'</div><h3>Employers by production branch</h3>'+branchHtml)}
  window.openEmployer=function(id){var e=employers.find(function(x){return x.id===id});if(!e)return;openModal('<h2>'+esc(e.name)+'</h2><p class="sub">'+esc(e.type||'')+' • '+esc(e.region||'')+'</p><p>'+esc(e.bestUse||'Research lead')+'</p><p>'+plainLink((e.links&&(e.links.apply||e.links.careers))?'Apply / careers':'Company website / contact',bestLink(e))+'</p>')};
  window.openLocalEl=function(el){if(el)window.openLocal(el.getAttribute('data-local'),el.getAttribute('data-district'))};
  window.openLocal=function(localId,district){var l=iatseLocals.find(function(x){return String(x.local)===String(localId)&&String(x.district)===String(district)});if(!l)return;var states=(l.states||[]).join(', ');openModal('<h2>IATSE Local '+esc(l.local)+'</h2><p class="sub">'+esc(l.district)+' • '+esc(l.jurisdiction)+'</p><div class="modalgrid"><div class="detail"><b>Directory description</b><br>'+esc(l.craft||'Multiple crafts — verify with the official source')+'</div><div class="detail"><b>Derived family</b><br>'+esc(iatseFamily(l))+'</div>'+(states?'<div class="detail"><b>Snapshot area</b><br>'+esc(states)+'</div>':'')+'<div class="detail"><b>Official sources</b><br><a href="sources.html#iatse-sources" onclick="event.stopPropagation()">Open the IATSE source register →</a></div></div><h3>Research this local</h3><ol class="iatse-steps"><li><b>Confirm craft and geography.</b> This snapshot description does not establish current jurisdiction for a particular event, venue, employer, or classification.</li><li><b>Open the current official directory.</b> The Atlas list is a partial snapshot dated 2026-06-21; current organizations and routes may differ.</li><li><b>Find separate public routes.</b> Work or referral registration, membership, apprenticeship or training, and workplace organizing are not interchangeable.</li><li><b>Verify current requirements.</b> Intake timing, eligibility, skills evidence, availability rules, costs, and training requirements can be local and time-sensitive.</li><li><b>Confirm the employment route.</b> A local listing or referral does not identify the employer of record, promise a call, or guarantee a schedule.</li></ol><div class="notice">Production Atlas is an independent research index. It does not decide membership, jurisdiction, dispatch priority, employment, or event-specific labor assignment.</div>')};
  window.openBranch=function(id){var b=branches.find(function(x){return x.id===id});if(!b)return;ensureBranchResearch().then(function(){var records=branchIndex.records.filter(function(r){return r.branchId===id});var cards=records.map(function(r){var o=opportunities.find(function(x){return norm(x.id)===norm(r.opportunityId)||norm(x.name)===norm(r.opportunityName)})||{id:r.opportunityId,name:r.opportunityName,departments:[id]};return '<div style="margin:0 0 4px"><p class="sub" style="margin:0 0 4px;color:var(--muted)"><b>'+esc(o.name||r.opportunityName||r.opportunityId)+'</b></p>'+branchCard(o,id)+'</div>'}).join('');var general=matchingEmployers(id).slice(0,12).map(employerRow).join('');openModal('<h2>'+esc(b.name)+'</h2><h3>Employers by festival</h3>'+(cards||'<p class="sub">No festival-specific companies listed yet.</p>')+(general?'<h3>Industry companies in this branch</h3><ul style="margin:0;padding:0">'+general+'</ul>':''))})};

  var _lastFocus=null;
  function openModal(html){var modal=$('#modal'),content=$('#modalContent');if(!modal||!content)return;content.innerHTML=html;modal.classList.add('open');modal.setAttribute('role','dialog');modal.setAttribute('aria-modal','true');var heading=content.querySelector('h1,h2,h3');if(heading){heading.id=heading.id||'modal-title';modal.setAttribute('aria-labelledby',heading.id)}else{modal.removeAttribute('aria-labelledby')}_lastFocus=document.activeElement;var box=modal.querySelector('.modalbox');if(box){box.setAttribute('tabindex','-1');box.focus()}
    if(!modal.dataset.trapBound){modal.dataset.trapBound='true';modal.addEventListener('keydown',function(e){if(e.key!=='Tab')return;var nodes=modal.querySelectorAll('a[href],button:not([disabled]),input,select,textarea,[tabindex]:not([tabindex="-1"])');if(!nodes.length)return;var first=nodes[0],last=nodes[nodes.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}})}}
  window.openModal=openModal;
  window.closeModal=function(){var modal=$('#modal');if(modal){modal.classList.remove('open');modal.removeAttribute('aria-modal')}if(_lastFocus&&typeof _lastFocus.focus==='function')_lastFocus.focus();_lastFocus=null};

  var EXTERNAL_RENDER_PAGES={calendar:1,map:1,employers:1,guide:1};
  function renderPage(){var page=document.body.dataset.page;if(EXTERNAL_RENDER_PAGES[page])return;({home:renderHome,opportunities:renderOpportunities,iatse:renderIatse,matrix:renderMatrix,branches:renderBranches,analytics:renderAnalytics,sources:renderSources,schedule:renderSchedule}[page]||renderHome)()}
  window.renderPage=renderPage;
  function autoFilterToCurrentMonth(){var monthSelect=$('#monthFilter');if(!monthSelect)return;var now=new Date();var currentMonth=String(now.getMonth()+1);if(monthSelect.value===currentMonth)return;monthSelect.value=currentMonth;monthSelect.dispatchEvent(new Event('change',{bubbles:true}));renderPage()}
  function init(){branches=window.RESOURCE_BRANCHES||[];allOpportunities=window.RESOURCE_OPPORTUNITIES||[];employers=window.RESOURCE_EMPLOYERS||[];iatseLocals=((window.IATSE_US_LOCAL_DIRECTORY||{}).locals)||[];opportunities=allOpportunities.filter(function(o){if(o.publishSafety==='do_not_publish'||o.visibility==='do_not_publish')return false;if(o.visibleInActive2026View===true)return true;if(o.defaultPublicCycleHidden===true&&Number(o.eventYear||o.publicCycleYear||0)>2026)return true;return false}).map(classify);window.branches=branches;window.employers=employers;window.scopedOpportunities=opportunities;window.iatseLocals=iatseLocals;fillFilters();applyUrlFilters();var modal=$('#modal');if(modal)modal.addEventListener('click',function(e){if(e.target.id==='modal')window.closeModal()});document.addEventListener('keydown',function(e){if(e.key==='Escape'){var m=$('#modal');if(m&&m.classList.contains('open'))window.closeModal()}if(e.key!=='Enter'&&e.key!==' '&&e.key!=='Spacebar')return;if(!e.target||!e.target.closest)return;var el=e.target.closest('[data-keyclick]');if(!el)return;var tag=e.target.tagName;if((tag==='A'||tag==='BUTTON'||tag==='INPUT'||tag==='SELECT'||tag==='TEXTAREA')&&e.target!==el)return;e.preventDefault();el.click()});var page=document.body.dataset.page;var branchPages={home:1,branches:1,sources:1,analytics:1,opportunities:1};if(branchPages[page])renderPage();ensureBranchResearch().then(function(){if(branchPages[page])renderPage()})}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();
