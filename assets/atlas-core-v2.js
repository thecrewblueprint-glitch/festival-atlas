(function(){
  var MONTHS=['January','February','March','April','May','June','July','August','September','October','November','December'];
  var branchFiles=[
    'branch-research-batch-001-staging.js','branch-research-batch-002-staging.js','branch-research-batch-003-staging.js','branch-research-batch-004-staging.js','branch-research-batch-005-staging.js',
    'branch-research-batch-001-rigging.js','branch-research-batch-002-rigging.js','branch-research-batch-003-rigging.js','branch-research-batch-004-rigging.js','branch-research-batch-005-rigging.js',
    'branch-research-batch-001-lighting.js','branch-research-batch-002-lighting.js','branch-research-batch-003-lighting.js','branch-research-batch-004-lighting.js','branch-research-batch-005-lighting.js',
    'branch-research-batch-001-audio.js','branch-research-batch-002-audio.js','branch-research-batch-003-audio.js','branch-research-batch-004-audio.js','branch-research-batch-005-audio.js',
    'branch-research-batch-001-video-led.js','branch-research-batch-002-video-led.js','branch-research-batch-003-video-led.js'
  ];
  var branches=[],opportunities=[],allOpportunities=[],employers=[],iatseLocals=[],branchIndex={records:[],byKey:{}};
  function $(s){return document.querySelector(s)}
  function $$(s){return Array.prototype.slice.call(document.querySelectorAll(s))}
  function esc(v){return String(v==null?'':v).replace(/[&<>'"]/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]})}
  function norm(v){return String(v||'').toLowerCase().replace(/&/g,'and').replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'')}
  function label(v){return String(v||'unknown').replaceAll('_',' ')}
  function chip(t,g){return '<span class="chip '+(g||'')+'">'+esc(t)+'</span>'}
  function linkChip(t,u,g){return u?'<a class="chip '+(g||'')+'" href="'+esc(u)+'" target="_blank" rel="noopener" onclick="event.stopPropagation()">'+esc(t)+' ↗</a>':''}
  function bestLink(e){var l=e.links||{};return l.apply||l.careers||l.directory||l.homepage||''}
  function text(o){return JSON.stringify(o||{}).toLowerCase()}
  function uniq(a){return Array.from(new Set(a)).filter(Boolean).sort()}
  function branchName(id){var b=branches.find(function(x){return x.id===id});return b?b.name:id}
  function loadScript(src){return new Promise(function(resolve){if($('script[data-loader="'+src+'"]'))return resolve();var s=document.createElement('script');s.src=src;s.async=false;s.dataset.loader=src;s.onload=resolve;s.onerror=function(){console.warn('Could not load',src);resolve()};document.head.appendChild(s)})}
  function loadBranchResearch(){return branchFiles.reduce(function(p,f){return p.then(function(){return loadScript('data/packages/'+f+'?v=multipage2')})},Promise.resolve()).then(buildBranchIndex)}
  function buildBranchIndex(){var records=[];Object.keys(window).forEach(function(k){if(!/^OPPORTUNITY_BRANCH_RESEARCH_BATCH_/.test(k))return;var d=window[k];if(!d||!Array.isArray(d.targets))return;d.targets.forEach(function(t){records.push(Object.assign({},t,{branchId:t.branchId||d.branchId,branchName:t.branchName||d.branchName,batchId:d.batchId,datasetKey:k}))})});branchIndex={records:records,byKey:{}};records.forEach(function(r){branchIndex.byKey[norm(r.opportunityId)+'::'+r.branchId]=r;branchIndex.byKey[norm(r.opportunityName)+'::'+r.branchId]=r})}
  function classify(o){var has=!!o.active2026SourceUrl;return Object.assign({sourceType:has?'public_secondary_source':'user_field_note',visibility:'public',confidence:has?'likely':'unverified',publishSafety:'public_safe',nextHumanAction:'Verify vendors, labor route, lodging, travel, and per diem before outreach.',intelligence:{publicSources:has?[{label:'active status source',url:o.active2026SourceUrl}]:[],fieldNotes:[],crewReferrals:[],privateContacts:[],doNotPublish:[]}},o)}
  function findBranchRecord(o,branchId){return branchIndex.byKey[norm(o.id)+'::'+branchId]||branchIndex.byKey[norm(o.name)+'::'+branchId]||null}
  function filterValues(){return{q:(($('#q')||{}).value||'').trim().toLowerCase(),branch:(($('#branchFilter')||{}).value||''),region:(($('#regionFilter')||{}).value||''),month:(($('#monthFilter')||{}).value||''),type:(($('#employerTypeFilter')||{}).value||'')}}
  function activeOpportunities(){var f=filterValues();return opportunities.filter(function(o){return(!f.q||text(o).includes(f.q)||(o.departments||[]).some(function(d){return branchName(d).toLowerCase().includes(f.q)}))&&(!f.branch||(o.departments||[]).includes(f.branch))&&(!f.region||o.region===f.region)&&(!f.month||String(o.month)===f.month)})}
  function activeEmployers(){var f=filterValues();return employers.filter(function(e){return(!f.q||text(e).includes(f.q)||(e.departments||[]).some(function(d){return branchName(d).toLowerCase().includes(f.q)}))&&(!f.branch||(e.departments||[]).includes(f.branch))&&(!f.region||String(e.region||'').includes(f.region)||e.region===f.region)&&(!f.type||e.type===f.type)})}
  function activeLocals(){var f=filterValues();return iatseLocals.filter(function(l){return(!f.q||text(l).includes(f.q))&&(!f.region||String(l.jurisdiction||'').includes(f.region)||String(l.states||'').includes(f.region)||l.district===f.region)})}
  function matchingEmployers(branchId){return employers.filter(function(e){return(e.departments||[]).includes(branchId)})}
  function matchingOpportunities(branchId){return opportunities.filter(function(o){return(o.departments||[]).includes(branchId)})}
  function fillFilters(){if(!$('#filters'))return;branches.forEach(function(b){var s=$('#branchFilter');if(s)s.innerHTML+='<option value="'+esc(b.id)+'">'+esc(b.name)+'</option>'});uniq(opportunities.map(function(o){return o.region}).concat(employers.map(function(e){return e.region}).concat(iatseLocals.flatMap(function(l){return l.states||[]}).concat(iatseLocals.map(function(l){return l.district}))))).forEach(function(r){var s=$('#regionFilter');if(s)s.innerHTML+='<option>'+esc(r)+'</option>'});MONTHS.forEach(function(m,i){var s=$('#monthFilter');if(s)s.innerHTML+='<option value="'+(i+1)+'">'+m+'</option>'});uniq(employers.map(function(e){return e.type})).forEach(function(t){var s=$('#employerTypeFilter');if(s)s.innerHTML+='<option>'+esc(t)+'</option>'});$$('#filters input,#filters select').forEach(function(el){el.addEventListener('input',renderPage)});var reset=$('#reset');if(reset)reset.onclick=function(){$$('#filters input,#filters select').forEach(function(el){el.value=''});renderPage()}}
  function badges(o){return chip(label(o.activeStatus||o.status||'active'),'warn')+chip('value '+(o.longTermValueScore||0),'gray')+chip('lodging '+label((o.accommodation||{}).lodgingLikely),'gray')}
  function opportunityCard(o){return '<article class="card click" onclick="openOpportunity(\''+esc(o.id)+'\')"><h3>'+esc(o.name)+'</h3><div class="sub">'+esc(o.city)+', '+esc(o.state)+' • '+esc(o.region)+' • '+esc(MONTHS[(o.month||1)-1]||'Unknown')+'</div><p><b>Date:</b> '+esc(o.startDate||'verify')+(o.endDate?' to '+esc(o.endDate):'')+'</p><p><b>Venue:</b> '+esc(o.venue||'verify')+'</p><div class="chips">'+badges(o)+'</div><p><b>Next:</b> '+esc((o.nextResearchActions||[])[0]||o.nextHumanAction||'Verify before outreach')+'</p></article>'}
  function employerCard(e){return '<article class="card click" onclick="openEmployer(\''+esc(e.id)+'\')"><h3>'+esc(e.name)+'</h3><div class="sub">'+esc(e.type)+' • '+esc(e.region)+'</div><div class="chips">'+(e.departments||[]).slice(0,7).map(function(d){return chip(branchName(d),'gray')}).join('')+'</div><p>'+esc(e.bestUse||'U.S. employer/vendor research lead.')+'</p><div class="chips">'+linkChip(e.linkStatus==='homepage_fallback'?'Company homepage':'Apply / careers',bestLink(e))+'</div></article>'}
  function iatseCard(l){return '<article class="card click" onclick="openLocal(\''+esc(l.local)+'\',\''+esc(l.district)+'\')"><h3>IATSE Local '+esc(l.local)+'</h3><div class="sub">'+esc(l.district)+' • '+esc(l.jurisdiction)+'</div><div class="chips">'+chip(l.craft)+(l.states||[]).map(function(s){return chip(s,'gray')}).join('')+'</div><p><b>Use case:</b> check possible local jurisdiction and contact route.</p></article>'}
  function renderHome(){var el=$('#app');if(!el)return;el.innerHTML='<h2>Dashboard Overview</h2><div class="stats"><div class="stat"><b>'+opportunities.length+'</b><span>active opportunities</span></div><div class="stat"><b>'+employers.length+'</b><span>employer leads</span></div><div class="stat"><b>'+iatseLocals.length+'</b><span>IATSE records</span></div><div class="stat"><b>'+branches.length+'</b><span>branches</span></div><div class="stat"><b>'+branchIndex.records.length+'</b><span>branch records</span></div></div>'}
  function renderCalendar(){var data=activeOpportunities(),el=$('#app');if(!el)return;el.innerHTML='<h2>Long-Term Opportunity Calendar</h2><p class="lead">Month-by-month view of active public-safe work targets.</p><div class="calendar">'+MONTHS.map(function(m,i){var ev=data.filter(function(o){return Number(o.month)===i+1});return '<div class="month"><h3>'+m+' <span class="sub">'+ev.length+'</span></h3><div class="monthBody">'+(ev.length?ev.map(function(o){return '<div class="event" onclick="openOpportunity(\''+esc(o.id)+'\')"><b>'+esc(o.name)+'</b><small>'+esc(o.city)+', '+esc(o.state)+' • '+esc(label(o.opportunityType))+'</small></div>'}).join(''):'<div class="sub">No work targets in current filter.</div>')+'</div></div>'}).join('')+'</div>'}
  function renderOpportunities(){var el=$('#app'),data=activeOpportunities();if(el)el.innerHTML='<h2>Opportunity Profiles</h2><p class="lead">Compare events by city, venue, date, route value, and department coverage.</p><div class="grid">'+(data.length?data.map(opportunityCard).join(''):'<p>No opportunity profiles match the current filter.</p>')+'</div>'}
  function renderEmployers(){var el=$('#app'),data=activeEmployers();if(el)el.innerHTML='<h2>United States Employer and Vendor Leads</h2><p class="lead">General public employer and vendor routes. These are not event-specific confirmations.</p><div class="grid">'+(data.length?data.map(employerCard).join(''):'<p>No employer leads match.</p>')+'</div>'}
  function renderIatse(){var el=$('#app'),data=activeLocals();if(el)el.innerHTML='<h2>United States IATSE Local Directory</h2><p class="lead">Routing aid for jurisdiction research. Verify directly before outreach.</p><div class="grid">'+(data.length?data.map(iatseCard).join(''):'<p>No local records match.</p>')+'</div>'}
  function renderMatrix(){var el=$('#app');if(!el)return;var rows=branches.map(function(b){var emps=matchingEmployers(b.id).slice(0,10).map(function(e){return linkChip(e.name,bestLink(e))}).join(' ');return '<tr><td><b>'+esc(b.name)+'</b><br><span class="sub">'+esc(b.question)+'</span></td><td>'+esc((b.researchNeeds||[]).join(', '))+'</td><td>'+esc((b.workerFocus||[]).join(', '))+'</td><td>'+emps+'</td><td><button class="btn" onclick="openBranch(\''+esc(b.id)+'\')">Open branch</button></td></tr>'}).join('');el.innerHTML='<h2>Employer and Hiring Matrix</h2><div class="tablewrap"><table class="matrix"><thead><tr><th>Branch</th><th>Research needs</th><th>Worker focus</th><th>General leads</th><th>Detail</th></tr></thead><tbody>'+rows+'</tbody></table></div>'}
  function renderBranches(){var el=$('#app');if(!el)return;el.innerHTML='<h2>Production Branches</h2><p class="lead">Department dashboard. Open a branch for researched opportunities, route leads, and next actions.</p><div class="grid">'+branches.map(function(b){var count=branchIndex.records.filter(function(r){return r.branchId===b.id}).length;return '<article class="card click" onclick="openBranch(\''+esc(b.id)+'\')"><h3>'+esc(b.name)+'</h3><p><b>'+esc(b.question)+'</b></p><p>'+esc((b.researchNeeds||[]).join(', '))+'</p><div class="chips">'+chip(matchingOpportunities(b.id).length+' active targets','gray')+chip(count+' research records','gray')+chip(matchingEmployers(b.id).length+' employer leads','gray')+'</div></article>'}).join('')+'</div>'}
  function renderAnalytics(){var el=$('#app');if(!el)return;function counts(arr,fn){return arr.reduce(function(a,x){var k=fn(x)||'Unknown';a[k]=(a[k]||0)+1;return a},{})}function bars(obj){var max=Math.max(1,...Object.values(obj));return Object.entries(obj).sort(function(a,b){return b[1]-a[1]}).map(function(p){return '<div class="bar"><span>'+esc(p[0])+'</span><div class="track"><div class="fill" style="width:'+(p[1]/max*100)+'%"></div></div><span>'+p[1]+'</span></div>'}).join('')}el.innerHTML='<h2>Dataset Analytics</h2><div class="grid"><div class="card"><h3>Opportunities by region</h3>'+bars(counts(opportunities,function(o){return o.region}))+'</div><div class="card"><h3>Research records by branch</h3>'+bars(counts(branchIndex.records,function(r){return r.branchName||branchName(r.branchId)}))+'</div><div class="card"><h3>Employers by type</h3>'+bars(counts(employers,function(e){return e.type}))+'</div></div>'}
  function renderGuide(){var el=$('#app');if(!el)return;el.innerHTML='<h2>Guide for Use</h2><p class="lead">Production Atlas is a scouting dashboard. Use records as research leads until verified by current public or official sources.</p><div class="notice"><b>Core rule:</b> do not treat route leads as confirmed vendors.</div>'}
  function renderSources(){var el=$('#app');if(!el)return;var rows=[];opportunities.forEach(function(o){((o.intelligence||{}).publicSources||[]).forEach(function(s){if(s.url)rows.push([o.name,'Opportunity',s.label||'source',s.url])})});branchIndex.records.forEach(function(r){(r.sourceLinks||[]).forEach(function(s){if(s.url)rows.push([r.opportunityName||r.opportunityId,r.branchName||r.branchId,s.label||'source',s.url])})});el.innerHTML='<h2>Sources</h2><p class="lead">Organized public source list. Sources are kept here instead of inside popups so event and branch popups stay clean.</p><div class="tablewrap"><table class="matrix"><thead><tr><th>Item</th><th>Section</th><th>Source</th><th>Link</th></tr></thead><tbody>'+rows.map(function(r){return '<tr><td>'+esc(r[0])+'</td><td>'+esc(r[1])+'</td><td>'+esc(r[2])+'</td><td>'+linkChip('Open source',r[3])+'</td></tr>'}).join('')+'</tbody></table></div>'}
  function renderPage(){var p=document.body.dataset.page;({home:renderHome,calendar:renderCalendar,opportunities:renderOpportunities,employers:renderEmployers,iatse:renderIatse,matrix:renderMatrix,branches:renderBranches,analytics:renderAnalytics,guide:renderGuide,sources:renderSources}[p]||renderHome)()}
  function branchCard(o,branchId){var b=branches.find(function(x){return x.id===branchId})||{name:branchId,question:'Verify route'};var r=findBranchRecord(o,branchId);if(!r)return '<div class="branch"><h4>'+esc(b.name)+'</h4><p class="sub">'+esc(b.question)+'</p><div class="chips">'+chip('No event-specific branch record yet','warn')+'</div></div>';var leads=(r.publicLeads||[]).slice(0,8).map(function(id){var e=employers.find(function(x){return x.id===id});return e?linkChip(e.name,bestLink(e),'gray'):chip(id,'gray')}).join('');return '<div class="branch"><h4>'+esc(r.branchName||b.name)+'</h4><div class="chips">'+chip(label(r.status),'warn')+chip(label(r.confidence),'gray')+'</div><p><b>Likely route:</b> '+esc(r.branchDisplayText||r.evidenceSummary||'Research route stored.')+'</p><p class="sub"><b>Next:</b> '+esc(r.nextAction||'Verify before outreach.')+'</p><div class="chips">'+leads+'</div></div>'}
  window.openOpportunity=function(id){var o=opportunities.find(function(x){return x.id===id});if(!o)return;var branchHtml=(o.departments||[]).map(function(d){return branchCard(o,d)}).join('');openModal('<h2>'+esc(o.name)+'</h2><p class="sub">'+esc(o.city)+', '+esc(o.state)+' • '+esc(o.venue||'venue verify')+' • '+esc(o.startDate||'date verify')+(o.endDate?' to '+esc(o.endDate):'')+'</p><div class="chips">'+badges(o)+'</div><div class="modalgrid"><div class="detail"><b>Producer/promoter</b><br>'+esc((o.producer||{}).name||'verify')+'</div><div class="detail"><b>Work-year value</b><br>'+esc(o.longTermValueScore||0)+'/100</div><div class="detail"><b>Lodging</b><br>'+esc(label((o.accommodation||{}).lodgingLikely))+'</div><div class="detail"><b>Travel / per diem</b><br>Travel: '+esc(label((o.travelCompensation||{}).travelPaid))+'<br>Per diem: '+esc(label((o.travelCompensation||{}).perDiem))+'</div></div><p><b>Next human action:</b> '+esc(o.nextHumanAction||'Verify before outreach.')+'</p><h3>Mapped production branches</h3>'+branchHtml)}
  window.openEmployer=function(id){var e=employers.find(function(x){return x.id===id});if(!e)return;openModal('<h2>'+esc(e.name)+'</h2><p class="sub">'+esc(e.type)+' • '+esc(e.region)+'</p><p>'+esc(e.bestUse||'Research lead')+'</p><div class="chips">'+linkChip(e.linkStatus==='homepage_fallback'?'Company homepage':'Apply / careers',bestLink(e))+(e.departments||[]).map(function(d){return chip(branchName(d),'gray')}).join('')+'</div>')}
  window.openLocal=function(local,district){var l=iatseLocals.find(function(x){return String(x.local)===String(local)&&String(x.district)===String(district)});if(!l)return;openModal('<h2>IATSE Local '+esc(l.local)+'</h2><p class="sub">'+esc(l.district)+' • '+esc(l.jurisdiction)+'</p><div class="chips">'+chip(l.craft)+(l.states||[]).map(function(s){return chip(s,'gray')}).join('')+'</div><p>Use as a jurisdiction routing aid. Verify before outreach.</p>')}
  window.openBranch=function(id){var b=branches.find(function(x){return x.id===id});if(!b)return;var recs=branchIndex.records.filter(function(r){return r.branchId===id});var cards=recs.map(function(r){var fake=opportunities.find(function(o){return norm(o.id)===norm(r.opportunityId)||norm(o.name)===norm(r.opportunityName)})||{id:r.opportunityId,name:r.opportunityName};return branchCard(fake,id)}).join('');openModal('<h2>'+esc(b.name)+'</h2><p><b>'+esc(b.question)+'</b></p><div class="modalgrid"><div class="detail"><b>Event-specific records</b><br>'+recs.length+'</div><div class="detail"><b>General employer leads</b><br>'+matchingEmployers(id).length+'</div></div><h3>Research needs</h3><p>'+esc((b.researchNeeds||[]).join(', '))+'</p><h3>Worker focus</h3><p>'+esc((b.workerFocus||[]).join(', '))+'</p><h3>Event-specific branch records</h3>'+(cards||'<p class="sub">No records yet.</p>'))}
  function openModal(html){var m=$('#modal'),c=$('#modalContent');if(!m||!c)return;c.innerHTML=html;m.classList.add('open')} window.openModal=openModal; window.closeModal=function(){var m=$('#modal');if(m)m.classList.remove('open')}
  function init(){branches=window.RESOURCE_BRANCHES||[];allOpportunities=window.RESOURCE_OPPORTUNITIES||[];employers=window.RESOURCE_EMPLOYERS||[];iatseLocals=((window.IATSE_US_LOCAL_DIRECTORY||{}).locals)||[];opportunities=allOpportunities.filter(function(o){return o.visibleInActive2026View===true&&o.publishSafety!=='do_not_publish'&&o.visibility!=='do_not_publish'}).map(classify);window.branches=branches;window.employers=employers;window.scopedOpportunities=opportunities;window.iatseLocals=iatseLocals;fillFilters();renderPage();var modal=$('#modal');if(modal)modal.addEventListener('click',function(e){if(e.target.id==='modal')window.closeModal()})}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',function(){loadBranchResearch().then(init)});else loadBranchResearch().then(init);
})();
